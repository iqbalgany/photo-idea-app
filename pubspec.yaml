name: photo_idea_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  d_input: ^0.5.1
  extended_image: ^9.1.0
  fd_log: ^0.0.8
  flutter:
    sdk: flutter
  gap: ^3.0.1
  get: ^4.7.2
  get_it: ^8.0.3
  google_fonts: ^6.2.1
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
